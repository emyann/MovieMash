'use strict';
(function(){
	// Declare app level module which depends on views, and components
angular
	.module('movieMash', [
  		'ui.router',
  		'ngAnimate',
  		'LocalStorageModule',
  		'firebase'
	]);

})();
